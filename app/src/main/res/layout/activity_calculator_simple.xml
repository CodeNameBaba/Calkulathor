<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center"
    android:background="@drawable/bg_gradient"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Display -->
    <TextView
        android:id="@+id/tvCalcDisplay"
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:gravity="end|center_vertical"
        android:padding="20dp"
        android:textSize="32sp"
        android:textColor="@color/white"
        android:background="@drawable/display_bg"
        android:elevation="6dp"
        android:text="" />

    <!-- Buttons Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="18dp">

        <!-- Row 1 -->
        <LinearLayout style="@style/CalcRow">
            <Button style="@style/CalcBtn" android:id="@+id/btn7" android:text="7"/>
            <Button style="@style/CalcBtn" android:id="@+id/btn8" android:text="8"/>
            <Button style="@style/CalcBtn" android:id="@+id/btn9" android:text="9"/>
            <Button style="@style/CalcOpBtn" android:id="@+id/btnDiv" android:text="/"/>
        </LinearLayout>

        <!-- Row 2 -->
        <LinearLayout style="@style/CalcRow">
            <Button style="@style/CalcBtn" android:id="@+id/btn4" android:text="4"/>
            <Button style="@style/CalcBtn" android:id="@+id/btn5" android:text="5"/>
            <Button style="@style/CalcBtn" android:id="@+id/btn6" android:text="6"/>
            <Button style="@style/CalcOpBtn" android:id="@+id/btnMul" android:text="*"/>
        </LinearLayout>

        <!-- Row 3 -->
        <LinearLayout style="@style/CalcRow">
            <Button style="@style/CalcBtn" android:id="@+id/btn1" android:text="1"/>
            <Button style="@style/CalcBtn" android:id="@+id/btn2" android:text="2"/>
            <Button style="@style/CalcBtn" android:id="@+id/btn3" android:text="3"/>
            <Button style="@style/CalcOpBtn" android:id="@+id/btnMinus" android:text="-"/>
        </LinearLayout>

        <!-- Row 4 -->
        <LinearLayout style="@style/CalcRow">
            <Button style="@style/CalcBtn" android:id="@+id/btn0" android:text="0"/>
            <Button style="@style/CalcBtn" android:id="@+id/btnDot" android:text="."/>
            <Button style="@style/CalcEqBtn" android:id="@+id/btnEquals" android:text="="/>
            <Button style="@style/CalcOpBtn" android:id="@+id/btnPlus" android:text="+"/>
        </LinearLayout>

    </LinearLayout>

    <!-- Clear Button -->
    <Button
        android:id="@+id/btnClear"
        style="@style/ClearBtn"
        android:text="@string/clear" />
</LinearLayout>
