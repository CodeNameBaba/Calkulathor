<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Calkulathor">

        <!-- Main Activity (Launcher) -->
        <activity
            android:name=".MainMenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".SelectModelActivity"
            android:exported="false" />

        <activity
            android:name=".SelectEndingMediaActivity"
            android:exported="false" />



        <activity
            android:name=".HelloWorldCalculatorActivity"
            android:exported="false" />

        <activity
            android:name=".SmartestCalculatorActivity"
            android:exported="false" />

        <activity
            android:name=".RemoveOSCalculatorActivity"
            android:exported="false" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".FullscreenCrashActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:exported="false" />

    </application>

</manifest>